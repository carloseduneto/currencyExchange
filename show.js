// window.addEventListener('scroll', function() {
//     var container = document.getElementById('container');
//     var centralizado = document.getElementById('centralizado');
//     var containerRect = container.getBoundingClientRect();

//     var top = Math.max(containerRect.top, 0);
//     var left = Math.max(containerRect.left, 0);
//     var bottom = Math.min(containerRect.bottom, window.innerHeight);
//     var right = Math.min(containerRect.right, window.innerWidth);

//     centralizado.style.top = ((top + bottom) / 2) + 'px';
//     centralizado.style.left = ((left + right) / 2) + 'px';
// });